<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Allows changing locales -->
    <uses-permission
        android:name="android.permission.CHANGE_CONFIGURATION"
        tools:ignore="ProtectedPermissions" />

    <!-- Allows changing SystemUI demo mode -->
    <uses-permission
        android:name="android.permission.DUMP"
        tools:ignore="ProtectedPermissions" />

    <!-- Hilt test applications are not initialized when package-replacement broadcasts arrive. -->
    <application android:icon="@mipmap/logo">
        <receiver
            android:name="com.futsch1.medtimer.Autostart"
            tools:node="remove" />
        <receiver
            android:name="com.futsch1.medtimer.TimeChangeReceiver"
            tools:node="remove" />
    </application>

</manifest>
